<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ShopEasy Loyalty Program{% endblock %}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <a href="/" class="logo">ShopEasy Loyalty</a>
            <ul class="nav-menu">
                {% if session.user_id %}
                    <li><a href="/dashboard">Dashboard</a></li>
                    <li><a href="/points/history">Points History</a></li>
                    <li><a href="/rewards">Rewards</a></li>
                    <li><a href="/rewards/my">My Rewards</a></li>
                    <li class="user-info">
                        <span>{{ session.user_name }}</span>
                        <a href="/logout" class="logout">Logout</a>
                    </li>
                {% else %}
                    <li><a href="/login">Login</a></li>
                    <li><a href="/register">Register</a></li>
                {% endif %}
            </ul>
        </div>
    </nav>

    <main class="container">
        {% if session.success %}
            <div class="alert alert-success">
                {{ session.success }}
                {% set session = session|merge({'success': null}) %}
            </div>
        {% endif %}
        
        {% if session.error %}
            <div class="alert alert-error">
                {{ session.error }}
                {% set session = session|merge({'error': null}) %}
            </div>
        {% endif %}

        {% block content %}{% endblock %}
    </main>

    <footer>
        <div class="container">
            <p>&copy; {{ "now"|date("Y") }} ShopEasy Loyalty Program</p>
        </div>
    </footer>
</body>
</html>