<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Product Catalog</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-8">

    <div class="max-w-6xl mx-auto">
        <h1 class="text-3xl font-bold mb-8 text-gray-800">Our Products</h1>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {% for product in products %}
                <div class="bg-white rounded-lg shadow-md overflow-hidden flex flex-col">
                   
                    <img src="{{ product.image_url }}" alt="{{ product.name }}" class="w-full h-48 object-cover">

                    <div class="p-4 flex-grow">
                     
                        <h2 class="text-xl font-semibold text-gray-900">{{ product.name }}</h2>
                        <span class="text-xs text-gray-400 uppercase font-bold">Ref: #{{ product.product_id }}</span>

                        <p class="mt-2 text-gray-600 text-sm">
                            {{ product.description|length > 100 ? product.description|slice(0, 100) ~ '...' : product.description }}
                        </p>
                    </div>

                    <div class="p-4 border-t border-gray-100 flex justify-between items-center">
                  
                        <span class="text-2xl font-bold text-indigo-600">${{ product.price|number_format(2) }}</span>
                        
                     
                        {% if product.stock_quantity > 0 %}
                            <span class="text-sm text-green-600 font-medium">In Stock ({{ product.stock_quantity }})</span>
                        {% else %}
                            <span class="text-sm text-red-500 font-medium">Out of Stock</span>
                        {% endif %}
                    </div>
                </div>
            {% else %}
                <p class="col-span-full text-center text-gray-500">No products found.</p>
            {% endfor %}
        </div>
    </div>

</body>
</html>

